apiVersion: v1
kind: Secret
metadata:
  name: chatbot-secret
  namespace: datahub-chatbot
type: Opaque
data:
  # echo -n "your-token" | base64
  DATAHUB_TOKEN: ""
  # echo -n "sk-..." | base64
  OPENAI_API_KEY: ""
  # echo -n "your-key" | base64
  COHERE_API_KEY: ""
