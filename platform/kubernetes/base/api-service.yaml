apiVersion: v1
kind: Service
metadata:
  name: chatbot-api
  namespace: datahub-chatbot
  labels:
    app: chatbot-api
spec:
  selector:
    app: chatbot-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  type: ClusterIP
